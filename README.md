# Approvals API

One-to-one HTTP API adapter for `module-automation-approvals`. Routes are versioned under `/api/v1/automation/approvals`, require authenticated team context, and delegate domain mutations to the core package.

